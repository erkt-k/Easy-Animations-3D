namespace EasyAnimationsEnums // TODO: Fix the namespace structure tomorrow
{   // Probably a placeholder :p I'm objectively too dumb to think of names

        public enum AxisOption2D {X, Y}
        public enum AxisOption3D {X, Y, Z}
        public enum MoveOption {V3, Uniform} 
}